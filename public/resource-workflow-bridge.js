(()=>{const run=()=>{const cards=[...document.querySelectorAll('.workflow-card')];if(!cards.length)return;const data=cards.map(card=>{const node=[...card.querySelectorAll('.workflow-node')];const get=k=>node.find(x=>x.querySelector('span')?.textContent.trim().toUpperCase()===k)?.querySelector('p')?.textContent.trim()||'';const tool=node.find(x=>x.querySelector('span')?.textContent.trim().toUpperCase()==='TOOL')?.querySelector('strong')?.textContent.trim()||'';let marker={};const raw=get('INPUT');if(raw.startsWith('[[SF_INPUTS]]')){try{marker=JSON.parse(raw.slice(12))}catch{}}return{title:card.querySelector('h3')?.textContent.trim()||'',tool,inputs:marker.inputs||[],settings:marker.settings||[],process:get('PROCESS / PROMPT'),output:get('OUTPUT'),next:get('NEXT STEP')}});let s=document.getElementById('sf-workflow-data');if(!s){s=document.createElement('script');s.id='sf-workflow-data';s.type='application/json';document.body.append(s)}s.textContent=JSON.stringify(data)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run()})();