(()=>{const localFallback='/skill-foundry-resource-icon.svg';const CACHE_KEY='sf-tool-logo-cache-v2';const CACHE_TTL=7*24*60*60*1000;const inFlight=new Map();const readCache=()=>{try{const raw=localStorage.getItem(CACHE_KEY);const data=raw?JSON.parse(raw):{};const now=Date.now();Object.keys(data).forEach(key=>{if(!data[key]||now-Number(data[key].savedAt||0)>CACHE_TTL)delete data[key]});return data}catch{return{}}};const writeCache=data=>{try{localStorage.setItem(CACHE_KEY,JSON.stringify(data))}catch{}};const cacheKey=url=>{try{return new URL(url,window.location.href).href.replace(/\/$/,'')}catch{return String(url||'')}};const websiteFallback=url=>{try{const target=new URL(url,window.location.href);if(target.hostname.endsWith('skillfoundryai.workers.dev'))return`${target.origin}/favicon.svg`;return`https://www.google.com/s2/favicons?domain=${encodeURIComponent(target.hostname)}&sz=128`}catch{return localFallback}};const imageQuality=src=>new Promise(resolve=>{if(!src)return resolve({src:'',width:0,height:0});const probe=new Image;probe.decoding='async';probe.referrerPolicy='no-referrer';probe.onload=()=>resolve({src,width:probe.naturalWidth||0,height:probe.naturalHeight||0});probe.onerror=()=>resolve({src,width:0,height:0});probe.src=src});const resolveLogo=async url=>{const key=cacheKey(url);if(!key)return localFallback;if(inFlight.has(key))return inFlight.get(key);const cached=readCache()[key];if(cached?.logoUrl)return cached.logoUrl;const promise=(async()=>{let logoUrl='';try{const response=await fetch(`/api/tool-logo?url=${encodeURIComponent(url)}`,{credentials:'same-origin',cache:'no-store'});if(response.ok){const data=await response.json();if(data.logoUrl&&!data.logoUrl.endsWith('/skill-foundry-resource-icon.svg'))logoUrl=data.logoUrl}}catch{}if(!logoUrl){try{const target=new URL(url,window.location.href);const candidates=[`${target.origin}/favicon.svg`,`${target.origin}/favicon.png`,`${target.origin}/favicon.ico`,`${target.origin}/apple-touch-icon.png`];for(const candidate of candidates){const result=await imageQuality(candidate);if(result.width>0&&result.height>0){logoUrl=result.src;break}}}catch{}}logoUrl=logoUrl||websiteFallback(url);const cache=readCache();cache[key]={logoUrl,savedAt:Date.now()};writeCache(cache);return logoUrl})().finally(()=>inFlight.delete(key));inFlight.set(key,promise);return promise};window.SkillFoundryToolLogo={resolve:resolveLogo};const originalFetch=window.fetch.bind(window);window.fetch=async(input,init)=>{try{const requestUrl=typeof input==='string'?input:input?.url;if(requestUrl){const parsed=new URL(requestUrl,window.location.href);if(parsed.pathname==='/api/tool-logo'){const target=parsed.searchParams.get('url');if(target){const logoUrl=await resolveLogo(target);return new Response(JSON.stringify({logoUrl}),{status:200,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})}}}}catch{}return originalFetch(input,init)};const mountToolLogos=()=>{const section=document.querySelector('.tools-section');if(!section)return;const subcopy=section.querySelector('.section-subcopy');if(subcopy)subcopy.textContent='The apps and AI tools used throughout this resource.';const list=section.querySelector('.tools-list');if(!list)return;list.classList.add('tool-logo-grid');const cards=[...list.querySelectorAll('.tool-card')];cards.forEach(card=>{const name=card.querySelector('h3')?.textContent?.trim()||'Tool';const purpose=card.querySelector('p')?.textContent?.trim()||'';const link=card.querySelector('.tool-link');const href=link?.href;if(!href)return;const anchor=document.createElement('a');anchor.className='tool-logo-link';anchor.href=href;anchor.target='_blank';anchor.rel='noopener noreferrer';anchor.setAttribute('aria-label',`Open ${name}`);anchor.title=name;anchor.dataset.toolName=name;anchor.dataset.toolPurpose=purpose;const image=document.createElement('img');image.className='tool-logo-image';image.src=(()=>{try{return`${new URL(href,window.location.href).origin}/favicon.svg`}catch{return localFallback}})();image.alt=`${name} logo`;image.width=56;image.height=56;image.loading='eager';image.decoding='async';image.referrerPolicy='no-referrer';image.addEventListener('error',()=>{if(image.src!==localFallback)image.src=localFallback},{once:true});anchor.appendChild(image);card.replaceWith(anchor);resolveLogo(href).then(src=>{if(src)image.src=src}).catch(()=>{})});section.querySelector('.affiliate-disclosure')?.remove();if(!document.querySelector('#sf-tool-logo-grid-style')){const style=document.createElement('style');style.id='sf-tool-logo-grid-style';style.textContent='.tool-logo-grid{display:grid;grid-template-columns:repeat(4,56px);gap:18px 16px;align-items:center;justify-content:start}.tool-logo-link{display:block;width:56px;height:56px;box-sizing:border-box;border:1px solid rgba(148,163,184,.34);border-radius:14px;background:transparent;text-decoration:none;overflow:hidden;transition:transform .18s ease,filter .18s ease,border-color .18s ease}.tool-logo-image{display:block;width:56px;height:56px;max-width:56px;max-height:56px;object-fit:cover;border-radius:13px}.tool-logo-link:hover,.tool-logo-link:focus-visible{transform:translateY(-2px) scale(1.03);filter:brightness(1.06);border-color:rgba(148,163,184,.5);outline:none}@media(max-width:360px){.tool-logo-grid{grid-template-columns:repeat(4,52px);gap:16px 12px}.tool-logo-link{width:52px;height:52px;border-radius:13px}.tool-logo-image{width:52px;height:52px;border-radius:12px}}';document.head.appendChild(style)}section.classList.add('sf-tools-ready')};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mountToolLogos,{once:true});else mountToolLogos()})();