const build=()=>{const section=document.querySelector('.workflow-section.sf-v7');if(!section||document.getElementById('sf-workflow-data'))return!1;const cards=[...section.querySelectorAll('.workflow-card')];if(!cards.length)return!1;const toolMap=new Map([...document.querySelectorAll('.tools-section .tool-card')].map(card=>{const name=card.querySelector('h3')?.textContent?.trim().toLowerCase()||'';const url=card.querySelector('a.tool-link')?.href||'';const purpose=card.querySelector('.tool-card-main p')?.textContent?.trim()||'';return[name,{url,purpose}]}));const data=cards.map(card=>{const nodes=[...card.querySelectorAll('.workflow-node')];const text=label=>nodes.find(n=>n.querySelector('span')?.textContent?.trim().toUpperCase()===label)?.querySelector('p,strong')?.textContent?.trim()||'';const tool=text('TOOL');const marker=text('INPUT');let inputs=[],settings=[],toolPurpose='';if(marker.startsWith('[[SF_INPUTS]]')){try{const x=JSON.parse(marker.slice(12));inputs=Array.isArray(x.inputs)?x.inputs:[];settings=Array.isArray(x.settings)?x.settings:[];toolPurpose=String(x.toolPurpose||'').trim()}catch{}}const t=toolMap.get(tool.toLowerCase())||{};return{title:card.querySelector('h3')?.textContent?.trim()||'',tool,toolUrl:t.url||'',toolPurpose:toolPurpose||t.purpose||tool,inputs,settings,input:marker.startsWith('[[SF_INPUTS]]')?'':marker,process:text('PROCESS / PROMPT'),output:text('OUTPUT'),next:text('NEXT STEP')}});const node=document.createElement('script');node.id='sf-workflow-data';node.type='application/json';node.textContent=JSON.stringify(data);document.body.append(node);return!0};await new Promise(resolve=>{let tries=0;const wait=()=>{if(build()||++tries>200)return resolve();setTimeout(wait,50)};wait()});